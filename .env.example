POSTGRES_DB=sms_platform
POSTGRES_USER=sms_admin
POSTGRES_PASSWORD=change_me
ONBOARDING_SERVER_PORT=8081
AUTH_SERVER_PORT=8082
GATEWAY_SERVER_PORT=8080
SCHOOL_OPERATIONS_SERVER_PORT=8083
MCP_SERVER_PORT=8084
FRONTEND_PORT=3000
VITE_API_BASE_URL=http://localhost:8080
VITE_MCP_BASE_URL=http://localhost:8084
VITE_SESSION_TIMEOUT_MINUTES=30

# JWT (required for api-gateway + auth-service)
JWT_SECRET_KEY=change_me_change_me_change_me_2026

# Optional: EMQX dashboard creds (dev only)
EMQX_DASHBOARD_USERNAME=admin
EMQX_DASHBOARD_PASSWORD=change_me

# MCP server (dev defaults)
MCP_REQUIRE_AUTH=true
MCP_ENABLE_DOCKER_INSIGHTS=false

# Optional: bootstrap a SUPER_ADMIN on auth-service startup (recommended for local dev)
BOOTSTRAP_SUPERADMIN_ENABLED=false
BOOTSTRAP_SUPERADMIN_EMAIL=superadmin@sms.local
BOOTSTRAP_SUPERADMIN_PASSWORD=change_me
BOOTSTRAP_SUPERADMIN_FULL_NAME=Platform Super Admin
