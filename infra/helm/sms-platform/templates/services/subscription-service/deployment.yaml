{{- if .Values.services.subscriptionService.enabled }}
{{ include "sms.serviceDeployment" (dict "root" . "service" .Values.services.subscriptionService "serviceName" "subscription-service") }}
{{- end }}
