apiVersion: v1
kind: Service
metadata:
  name: communication-service
spec:
  type: ClusterIP
  selector:
    app: communication-service
  ports:
    - name: http
      port: 8089
      targetPort: http
