apiVersion: v1
kind: Service
metadata:
  name: subscription-service
spec:
  type: ClusterIP
  selector:
    app: subscription-service
  ports:
    - name: http
      port: 8086
      targetPort: http
